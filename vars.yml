# ============================================================
# All configurable variables — edit before running
# ============================================================

# --- MySQL ---
mysql_root_password: "StrongRootPass123!"
mysql_db_name: "wordpress_db"
mysql_db_user: "wp_user"
mysql_db_password: "StrongWPPass123!"

# --- WordPress ---
wordpress_version: "latest"               # e.g. "6.5" or "latest"
wordpress_install_dir: "/var/www/html"
wordpress_site_url: "http://YOUR_EC2_PUBLIC_IP"
wordpress_site_title: "My WordPress Site"

# --- WordPress Admin ---
wordpress_admin_user: "admin"
wordpress_admin_password: "AdminPass123!"
wordpress_admin_email: "admin@example.com"

# --- PHP ---
php_version: "8.1"                        # Ubuntu 22 ships with PHP 8.1
